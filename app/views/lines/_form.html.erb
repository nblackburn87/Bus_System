<%= render("layouts/errors", :object => @line) %>

<%= form_for(@line) do |s| %>

  <div class="form-group">
    <%= s.label :name %>
    <%= s.text_field :name %>
    <%= s.collection_select(:id, Station.all, :id, :name) %>
  </div>

  <%= s.submit(:class => "btn btn-primary") %>
<% end %>
